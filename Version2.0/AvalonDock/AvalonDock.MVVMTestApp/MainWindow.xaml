<Window x:Class="AvalonDock.MVVMTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonDock="clr-namespace:AvalonDock;assembly=AvalonDock"
        xmlns:avalonDockControls="clr-namespace:AvalonDock.Controls;assembly=AvalonDock"
        xmlns:avalonDockLayout="clr-namespace:AvalonDock.Layout;assembly=AvalonDock"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="Documents">
                <MenuItem Header="Add" Click="OnAdd"/>
                <MenuItem Header="Remove" Click="OnRemove"/>
                <Separator/>
                <MenuItem Header="Clear" Click="OnClear"/>
            </MenuItem>
        </Menu>
        <avalonDock:DockingManager x:Name="dockManager" DocumentsSource="{Binding ViewModelItems}" Grid.Row="1">
            <avalonDock:DockingManager.DocumentTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding Mode=OneWay}"/>
                </DataTemplate>
            </avalonDock:DockingManager.DocumentTemplate>
            <avalonDock:DockingManager.DocumentHeaderTemplate>
                <DataTemplate>
                    <Border Background="Purple">
                        <TextBlock Text="{Binding Title}"/>
                    </Border>
                </DataTemplate>
            </avalonDock:DockingManager.DocumentHeaderTemplate>
            <avalonDockLayout:LayoutRoot>
                <avalonDockLayout:LayoutPanel>
                    <avalonDockLayout:LayoutDocumentPaneGroup>
                        <avalonDockLayout:LayoutDocumentPane>
                        </avalonDockLayout:LayoutDocumentPane>
                    </avalonDockLayout:LayoutDocumentPaneGroup>
                </avalonDockLayout:LayoutPanel>
            </avalonDockLayout:LayoutRoot>
        </avalonDock:DockingManager>
    </Grid>
</Window>
